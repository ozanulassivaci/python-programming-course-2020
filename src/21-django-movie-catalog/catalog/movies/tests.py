from django.test import TestCase

# Create your tests here.
# LEARNER NOTE: TestCase is Django's base class for automated tests --
# subclassing it and writing methods named test_* (run with
# `python manage.py test`) gives you a temporary, throwaway test
# database and helpers like self.client (a fake browser you can use to
# make requests against your views) and self.assertEqual(...) to check
# results. This file is currently just the empty scaffold `startapp`
# generates; no tests have been written yet for the movies app.
