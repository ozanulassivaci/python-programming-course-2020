from django.db import models

# Create your models here.
# The "pages" app only serves static content (home and about), so it
# has no database models of its own -- this file is left as the empty
# scaffold that `python manage.py startapp pages` generates. Compare
# with movies/models.py, which defines the Movie model, to see what
# this file would look like if pages did need to store data.
