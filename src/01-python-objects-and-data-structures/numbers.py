# The + operator between two int literals performs ordinary arithmetic
# addition (not string concatenation, since these are numbers, not text).
# 2 + 2 evaluates to 4, and print() writes that value to the console.
print(2+2)
