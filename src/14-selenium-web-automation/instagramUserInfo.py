# Fill in your own Instagram credentials here for instagram.py to use -
# instagram.py does "from instagramUserInfo import username, password"
# rather than hardcoding them directly, so real credentials never need to
# live inside the automation script itself.
username = "<enter_your_username>"
password = "<enter_your_password>"