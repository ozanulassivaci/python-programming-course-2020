# Credentials live in their own small module and are imported by github.py
# ("from githubUserInfo import username, password") rather than being typed
# directly into the automation script. That keeps real credentials out of
# files that get committed - fill in your own GitHub username/password
# below only on your own machine, and never commit real values here.
username = ""
password = ""