# Fill in your own Twitter/X credentials here - both twitter.py and
# twitter-test.py do "from twitterUserInfo import username, password"
# rather than hardcoding them directly, so real credentials never need to
# live inside the automation scripts themselves.
username = "<enter_your_username>"
password = "<enter_your_password>"